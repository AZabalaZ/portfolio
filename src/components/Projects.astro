---
const PROJECTS = [
  {
    title: 'MEALMATE',
    description: 'This is a project description',
    image: '/projects/svgl.webp',
    link: 'https://www.mealmateorg.me/',
    github: '#',
    tags: ['Ruby', 'Bootstrap', 'JavaScript', 'HTML', 'CSS', "Rails"]
  },
  {
    title: 'MEALMATE',
    description: 'This is a project description',
    image: '/projects/svgl.webp',
    link: 'https://www.mealmateorg.me/',
    github: '#',
    tags: ['Ruby', 'Bootstrap', 'JavaScript', 'HTML', 'CSS', "Rails"]
  }
]
---
{
  PROJECTS.map(({image, title, description, tags}) => (
    <article>
      <h3 class="text-2xl font-semibold text-yellow-200 mb-2">{title}</h3>
      <p class="text-lg mb-4 text-pretty">{description}</p>
      <ul class="flex gap-x-4 flex-row">
        {tags.map((tag) => (
          <li class="rounded-full mb-4 border border-white/10 flex justify-center items-center gap-x-2 py-1 px-2 lg:py-1 px-3 text-xs lg:text-base bg-white/5 hover:scale-110 hover:bg-white/10 transition">{tag}</li>
        ))}
      </ul>
      <img class="rounded shadow-2xl shadow-white/10" src={image} alt={title} />
    </article>
    ))
}
